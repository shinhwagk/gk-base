```scala
    def abc(a:Int = 1 ) = println(a)
    abc()
// 1
    abc(10)
//10
```
###自动在调用方法时，可以不指定有默认值的参数,但要注意有默认值参数的位置
```scala
 def abc(b:Int,a:Int = 1) = println(a)
 abc(10)
 //上面的执行可以通过
 def abc(a:Int = 1，b:Int) = println(a)
 abc(10)
 //这样就不可以,因为10是第一个参数的值，也就是给a参数赋上了值,但是b参数没有默认值，也没有在调用方法时指定他的值，所以会报错。
```

