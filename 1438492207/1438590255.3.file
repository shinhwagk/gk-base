```scala
    try {
      val summ = downFileWorkList.map(_.success).sum
      println("Generated SQL for max price column:\n" + summ.result.statements)
      val a = db.run(summ.result)
      val ab = Await.result(a,Duration.Inf).toList
      println(ab.head)
    }finally db.close()
//4
```